import { EntityDisplayPipe } from './entity-display.pipe';

describe('EntityDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
